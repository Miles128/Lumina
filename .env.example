# LLM (OpenAI-compatible). Leave empty to reuse ~/.hermes/config.yaml
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=

# Prompt gate: classify user input before agent loop
PROMPT_GATE_ENABLED=true
PROMPT_GATE_MIN_CONFIDENCE=0.6
