# FS naming + move + confirm diff — Plan

**Goal:** Ship items 2/3/4 from knowledge-work tool backlog.

- [x] Rename `ls` / `grep` / `glob` (+ aliases)
- [x] `move` tool with confirm
- [x] `confirmation_diff` API + confirm bubble `<pre>`
- [x] Tests + `pytest` / `ruff`
